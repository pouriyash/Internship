using Alamut.Utilities.Http;

namespace Internship.Web.Helpers
{
    public static class UserResolverServiceExtensions
    {
        //public static int GetUserId(this IUserResolverService userResolverService)
        //    => int.Parse(userResolverService.UserId);
    }
}